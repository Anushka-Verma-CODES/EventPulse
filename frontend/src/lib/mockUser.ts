// Temporary mock data — replace with the real authenticated user
// (from AuthContext / your login response) once that's wired up.
export const mockAttendee = {
  name: "Rajat Sharma",
  email: "rajat@example.com",
  initials: "RS",
};

export const mockUnreadNotifications = 3;
